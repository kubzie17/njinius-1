export const signup = `

`.trim()

export const login = `

`.trim()

export const userData = `

`.trim()

export const logout = `
a
`.trim()

export const errorHandling = `

`.trim()

export const setAccessToken = `

)
`.trim()
